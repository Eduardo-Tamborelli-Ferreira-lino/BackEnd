version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: MySQL_Connections
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 231231
      MYSQL_DATABASE: Prova MI77
    ports:
      - "3356:3306"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge