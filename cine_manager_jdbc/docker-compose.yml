version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: fleet_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: mysqlPW
      MYSQL_DATABASE: cinema_db
    ports:
      - "3356:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: